# Required for JWT signing (use a long random string in production).
JWT_SECRET=whatisthishbbfiusbfsb343bhb

# Optional; default in code is 7d.
JWT_EXPIRES_IN=7d

# SQL Server and other vars — keep your existing .env values.
